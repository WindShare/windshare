// Package admission owns relay resource policy and its accounting lifecycle.
// Signaling consumes the policy through a locally defined interface so protocol
// handling stays independent from deployment limits.
package admission
