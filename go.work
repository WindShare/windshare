// go.work is local-development glue: root resolves core locally, while the
// independent performance-evidence module resolves the root process owner.
// Release validation disables the workspace and consumes tagged modules.
go 1.26.5

use (
	.
	./core
	./internal/perfevidence
)
