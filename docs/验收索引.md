# R Refactor Acceptance Index

> Status: **not release-ready** (evidence synchronized 2026-07-23). `PASS` applies only to a completed command or subphase on the named frozen snapshot. A blocked or nonzero aggregate is never promoted by successful earlier stages.
>
> “Current source” below means the final implementation snapshot before this evidence-only documentation sync. D5 digest `3cec0556ff3acf79121f752b8837779c5d94bea5004be583f1e4ad533790cb08` and outer digest `d0a789b8e6e28bb3d0f2da417b05aab5bec5ee1744954ef9771fad4ffba5f1d0` identify that snapshot through different manifest algorithms. HEAD `324964ffdf6a802fb8c7235e11c8a79267106143` alone cannot distinguish dirty-workspace epochs.

## 1. Contract entry points

These links define the accepted semantics; the gate table below decides whether the current implementation has complete release evidence.

| Boundary | Reproducible contract evidence |
|---|---|
| Capability links, sealed sender objects, session frames, fragments, and vectors | [Go protocol contracts](../core/internal/protocolcontract/contract_test.go), [authentication contracts](../core/internal/protocolcontract/authentication_contract_test.go), [TypeScript consumer](../web/test/protocol/r0-contract.test.ts), and [vector inventory test](../core/internal/protocolcontract/inventory_test.go) |
| Progressive catalog, file-local content, selection, and durable output | [catalog store](../core/catalog/store_test.go), [content flow](../core/session/contentflow/protocol_test.go), [transfer job](../core/transfer/job_test.go), [filesystem output session](../core/osfs/output_session_test.go), and [Web persistent output](../web/test/output/persistent-tree.test.ts) |
| Exact-operation receiver authority | [runtime terminal ownership](../core/session/sessionruntime/peer_receiver_terminal_test.go), [bounded teardown/classification](../connectivity/v2peer/receiver_shutdown_test.go), [runtime-scope isolation](../connectivity/v2peer/receiver_runtime_scope_test.go), and [CLI admission policy](../cmd/windshare/internal/cli/get_peer_scope_test.go) prove exact-generation isolation, immutable post-cleanup publication, fail-closed residual retention, and text-free traces. A valid peer-scoped failure remains attempt-local; an authenticated malformed or cross-scope final is session-fatal and cannot resume relay admission. |
| Production boundaries and real transports | [Go forbidden gate](../scripts/ci/go-v1-forbidden.mjs), [Web forbidden gate](../scripts/ci/web-forbidden.mjs), [relay endpoint](../relay/signaling/v2endpoint/server_test.go), [Pion integration entry](../transport/webrtc/v2peer_receiver_integration_test.go), and [browser hot-switch entry](../web/e2e/v2-real-hot-switch.spec.ts) |

## 2. Validation evidence

| Surface | Snapshot | Exact verdict |
|---|---|---|
| Post-static coverage | Current D5 `3cec0556…` | **BLOCKED**, `make coverage` exit 2. Core and ordinary-root sweeps passed and produced diagnostic-only totals of 90.1% and 80.2%. WBEM quota failure occurred before a fresh D5 plan, four suite exits, zero-skip proof, merged root profile, or pinned package/total thresholds. This is neither a coverage PASS nor a threshold FAIL. Evidence: `.codex-work/p2a-authoritative-coverage.md`. |
| Earlier full coverage | Pre-static `c153ee76…` | **HISTORICAL ONLY.** The prior 90.0% core / 82.8% root result and four zero D5 exits are excluded from the current verdict because vector/static changes superseded its source identity. |
| Root/core Go gates | Pre-static `c153ee76…` | Root and core uncached ordinary tests, `make core-release`, and `make race` **PASS for that frozen snapshot**. The extracted prospective `core@v0.2.0` artifact passed tidy/verify/build/normal/race checks. Post-static coverage separately confirms current ordinary core/root sweeps; the pre-static race/release result is not a current aggregate verdict. |
| Vet and released-core resolution | Pre-static `c153ee76…` | Root/core `go vet` for Windows and Linux all **PASS**, but `make vet` overall **FAILS** at the final root `GOWORK=off` consumer build: published `core v0.1.1` lacks packages in the prospective `v0.2.0` artifact. No local `replace`, module edit, publication, or release was used to hide this sequencing blocker. |
| Web static gate | Outer `24e75c35…` | `make web` **PASS**: frozen install, ESLint, forced TypeScript build, Vite build, forbidden graph/bundle, and Vitest all ran; 63/63 files and 420/420 tests passed. |
| Final hygiene/lint/SLOC | Post-static handoff | `make hygiene` **PASS** with no `gopls` diagnostics; `make lint` **PASS** with zero root/core issues; `make sloc` **PASS** with 624 checked, 601 passed, 23 warnings, and zero failures. The final handoff did not publish a frozen digest or raw log, so this is not content-addressed evidence. |
| Protocol vectors | Current outer `d0a789b8…` | Deterministic regeneration and exact inventory each **PASS twice**, but `make vectors` overall **FAILS** (exit 2) because `core/testvectors/` is untracked. No gate-induced drift occurred. |
| Browser gate | Pre-static `c153ee76…` | **BLOCKED before tests.** Three-engine executable preflight passed, then the WBEM/CIM namespace query stalled; 0 browser and 0 Pion cases launched. The gate was bounded, interrupted, and not retried. Current Web performance reproduced the pre-test WBEM class of failure. |
| Go performance | Current D5 `3cec0556…` | **PASS**: 90/90 samples, 18/18 aggregates, five samples each, and all exercised semantic/resource invariants passed. See [Performance Baseline](性能基线.md). |
| Windows/Web performance | Current outer `d0a789b8…` / D5 `3cec0556…` | **BLOCKED**, command exit 1. Browser preflight, Web build, and bundle reporting passed, but WBEM quota failure occurred before any performance scenario sampled. No current Web/Pion trend files exist, so this is failure evidence, not a performance PASS. |

## 3. Remaining release blockers

- Stabilize the Windows WBEM host path, then obtain current-source D5 coverage with four suite exits, zero skips, merged root coverage, and pinned threshold results.
- Run current-source browser and Windows/Web/Pion performance scenarios through actual tests and sampling.
- Put `core/testvectors/` into the committed source state and obtain an overall `make vectors` PASS.
- Publish `core v0.2.0`, update the root dependency through the authorized two-stage release, and pass the root released-core `GOWORK=off` build.
- Pass final `make ci`; no current evidence claims that aggregate.

No validation run changed Windows Firewall, WBEM, or other system settings. No commit, publication, or release was performed.
