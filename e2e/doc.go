// Package e2e 是 M1a(CLI↔CLI over relay)的黑盒验收套件(执行计划 §11 T6.1):
// 编译真实的 wsrelay 与 windshare 二进制,以三进程往返为主判据,覆盖完整哈希
// 一致、选择性下载、断点续传、发送端断线重连、分享中改文件中止、空文件/空目录
// 物化、已存在同名拒绝、分离密钥、传输篡改与恶意清单拒收(§6.9/§6.11/§6.13)。
//
// 黑盒难以注入的路径(发送端 WS 断线、逐字节篡改、恶意清单构造)按 T6.1 的
// 许可降级为传输层/库级进程内测试,并在各自文件顶部注明理由。本包只含测试
// 代码;此文件仅为包声明与工具链留一处非测试锚点。
package e2e
