
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# go.work 入库(双模块本地开发依赖 workspace 解析,执行计划 §6.2),不忽略

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

test.sh
cover.out
TODO
*_gitignore*
*gitignore_*
.cursor/
.claude/

# Coverage output directories
web/coverage/
coverage_ws/

# Stray coverage profiles produced by unquoted pwsh args
# (`-coverprofile=$var` writes a literal `$var`; `-coverprofile=x.out`
# tokenizes as `x` + `.out`). Quote the whole argument to avoid new ones.
$*
root-cover
core-cover
cover
d4-core

# Playwright/benchmark run artifacts
test-results/

# Frontend build output (keep placeholder for go:embed)
web/dist/*
!web/dist/PLACEHOLDER.txt
!web/dist/.gitkeep

# Release build output (cross-compiled binaries)
dist/

config.yaml
tmp/
cmd/switch-a/config.yaml
tmpclaude-*
review_agent.md
.agents
.tmp

# 第三方参考克隆(仅本地查阅,不入库);锚定仓库根,勿殃及 transport/webrtc/
.trash
/.webrtc/
.orchestration

原始需求.md

# Node
node_modules/
.codex-work/
